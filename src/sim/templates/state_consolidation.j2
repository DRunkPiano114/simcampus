你是 {{ profile_name }} 的记忆整理助手。下面是最近 {{ lookback_days }} 天的候选聚类。
请判断每个 cluster 里哪些条目本质上是同一件事的不同表述，需要合并。

{% if memory_clusters %}
## Memory clusters
{% for cluster in memory_clusters %}
### Memory Cluster {{ loop.index }}：{{ cluster.topic }} / 涉及 {{ cluster.people | join("、") or "无" }}
{% for m in cluster.entries %}
- [{{ loop.index }}] [Day {{ m.day }}, importance {{ m.importance }}] {{ m.text }}
{% endfor %}
{% endfor %}
{% endif %}

{% if concern_clusters %}
## Concern clusters
{% for cluster in concern_clusters %}
### Concern Cluster {{ loop.index }}：topic={{ cluster.topic }}, people={{ cluster.people | join("、") or "无" }}
{% for c in cluster.entries %}
- [{{ loop.index }}] [Day {{ c.source_day }}, intensity {{ c.intensity }}] {{ c.text }}
{% endfor %}
{% endfor %}
{% endif %}

## 判断规则
1. 同一 cluster 里语义重叠 >= 70% 的条目 -> 合并
2. 同一 cluster 里讲两件不同事情 -> 保留分离（例："英语单词" 和 "物理压轴题" 都是学业焦虑但不是同一焦虑）
3. 合并后的 importance/intensity 取原 cluster 的最大值

**注意**：你只输出"哪几条该合并、合并后强度多少"的判断。**不需要也不允许重写 text 或 source_event**。

## 输出格式
对每个有合并的 cluster 返回 merge_groups 列表，每项含：
- `cluster_kind`: "memory" 或 "concern"
- `cluster_id`: cluster 在上面的编号（1-based）
- `source_indices`: 要合并的条目索引（cluster 内 1-based）
- `source_text_prefixes`: 对应每个 source_index 的条目 text 前 15 字
- `final_intensity_or_importance`: 合并后的数值

不需要合并的 cluster 不要在 merge_groups 里出现。如果所有 cluster 都不需要合并，返回空列表。
